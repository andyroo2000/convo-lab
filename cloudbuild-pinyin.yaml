steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/convolab-pinyin:latest'
      - '-f'
      - 'pinyin-service/Dockerfile'
      - 'pinyin-service/'

  # Push the image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/convolab-pinyin:latest'

images:
  - 'gcr.io/$PROJECT_ID/convolab-pinyin:latest'

timeout: '1200s'
