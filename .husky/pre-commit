echo "🔍 Running linting on staged files..."
npx lint-staged

# Only run tests if server files were changed
if git diff --cached --name-only | grep -q "^server/"; then
  echo "🧪 Running server tests (server files changed)..."
  cd server && npm run test:run
else
  echo "⏭️  Skipping server tests (no server files changed)"
fi
